# hasdera-elad

## Status

| | |
|---|---|
| **Domain** | `https://hasdera-elad.com` |
| **Pages URL** | `https://hasdera-elad.pages.dev` |
| **Storage mode** | `Standard` (`standard`) |
| **Storage account** | `jetvaults` |
| **Public storage** | `https://jetvaults.blob.core.windows.net/hasdera-elad/` |
| **Private storage** | `https://jetvaults.blob.core.windows.net/hasdera-elad-private/` |
| **Public container** | `hasdera-elad` |
| **Private container** | `hasdera-elad-private` |
| **Activated** | No |

## Nameservers

Set these at your domain registrar:

```
lia.ns.cloudflare.com
louis.ns.cloudflare.com
```

## Development

Edit files in `wwwroot/` and push to `main` - Cloudflare Pages auto-deploys.

Only the `wwwroot/` directory is served. Everything else stays in the repo.
